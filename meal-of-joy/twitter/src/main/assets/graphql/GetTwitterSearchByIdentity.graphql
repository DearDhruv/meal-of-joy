query ($q: String!) {
    twitter {
        search(q: $q) {
            user {
                created_at
                description
                followers_count
                id
                name
                profile_image_url
                screen_name
                tweets_count
                url
            }
            created_at
            text
            id
        }
    }
}
