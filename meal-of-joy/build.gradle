// Top-level build file where you can add configuration options common to all sub-projects/modules.
buildscript {
    apply from: 'app-versions.gradle'

    repositories {
        google()
        jcenter()
        maven { url "https://jitpack.io" }
    }
    dependencies {
        classpath "androidx.navigation:navigation-safe-args-gradle-plugin:$navigation_version"
        classpath 'com.android.tools.build:gradle:4.2.0-alpha03'
        classpath "com.google.gms:google-services:4.3.3"
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlinInfo.kotlin_version"
        classpath "org.jetbrains.kotlin:kotlin-serialization:$kotlinInfo.kotlin_version"
        classpath "com.google.firebase:firebase-crashlytics-gradle:2.2.0"

        // NOTE: Do not place your application dependencies here; they belong
        // in the individual module build.gradle files
    }
}

allprojects {
    repositories {
        google()
        jcenter()
        maven { url "https://jitpack.io" }
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}